# Supply-chain cooldown: never install a dependency version published less than
# 7 days ago (applies to transitive deps too, at resolution time).
# Requires npm >= 11.10.0; silently ignored on older npm. Bypass an urgent fix
# with `npm install --min-release-age=0`. Does not affect frozen `npm ci`.
min-release-age=7
